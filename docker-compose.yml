version: "2"
volumes:
    wallet_build: {}

services:
    web-wallet-prod:
        build:
            context: .
            args:
                - PLAYCHAIN=prod
        image: playchain-web-wallet-prod
        network_mode: host
        mem_limit: 2g

